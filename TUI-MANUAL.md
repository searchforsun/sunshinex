# SunshineX TUI 操作手册

交互式会话终端（TUI）对标 Claude Code 的使用方式：在终端里用自然语言下达任务，模型流式作答、工具实时执行、写操作经审批裁决、复杂目标先规划后执行。本文所有行为均经真实模型实测校验。

---

## 1. 启动

前置条件：

- Node.js ≥ 22.9（工程 engines 约束）
- 项目根目录 `.env` 已配置模型端点（缺省会退化为不可用的 OpenAI 缺省配置）：

```bash
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4   # 任意 OpenAI 协议兼容端点
OPENAI_MODEL=glm-5.3-flash
```

启动命令（项目根目录执行，包管理器统一 pnpm）：

```bash
pnpm cli tui                # 在当前目录打开会话（root=process.cwd()）
pnpm cli tui <dir>          # 在指定目录打开会话（项目空间模式）
pnpm cli tui --mode=manual  # 指定权限模式（缺省 manual）
```

> `pnpm cli` 脚本内置 `tsc` 自动构建并自动装载 `.env`（已导出的环境变量优先），无需单独 build。
> 模型缺省走 OpenAI 协议适配器；`--model=scripted|stub` 可切内置假模型（离线演示/测试用）。

## 2. 界面布局

```text
✻ SunshineX TUI v0.1.0 · model glm-5.3-flash
  /help 查看命令 · /plan 先规划后执行 · Tab 翻阅历史          ← 启动横幅（窄终端 <40 列降级为单行）
░ 帮我创建 note.txt，内容写 hello ░                       ← 消息流（整行底色带）
✻ Pondering… (8s · ↑1.2k tokens)                          ← 运行态活动行（思考/执行中）
  ✻ 思考流：灰色斜体末 6 行滚动                               ← 思考内容（端点回传 reasoning 时）
⏺ [WRITE] note.txt                                        ← 工具调用行（工具名高亮）
  ⎿ ✓ 42 bytes                                            ← 工具结果行（成功绿/失败红，默认折叠为单行摘要，全文经 Tab 翻阅展开）
### 已创建                                                     ← 助手答复（Markdown 结构化排版）
已创建 note.txt，内容为 hello。
┌ 审批 req-1（write） ─────────┐                          ← 审批卡（出现时拦截输入）
│ write note.txt               │
│ y 放行一次 · a 本会话放行 · n 拒绝 │
└──────────────────────────────┘
╭─ ❯ 输入任务，Enter 发送 · /help 查看命令 ▊ ─╮            ← 常驻边框输入框
╰────────────────────────────────────────────╯
 ↑1.2k tokens · 8.3s · model glm-5.3-flash · runs 5 · 缓存 83% · 空闲   ← 状态栏（/plan 执行时此处含「待办 n/N」进度）
```

状态栏口径：`↑本轮 tokens`（本轮 submit 周期 provider 上报的用量累计，未上报显示 0）· `耗时`（本轮运行秒数，未运行不显示）· `model`（当前模型名）· `runs`（账本运行次数）· `缓存`（本轮 prompt 缓存命中率，分子分母同源于 usage 事件；本轮无 usage 回传时回退会话级统计）· 待办进度（`/plan` 计划执行时显示）· 状态词（空闲/运行中/等待审批/待确认计划/出错）。运行态在消息区末尾显示活动行（帧动画 + 英文动词轮换 + 耗时 + 本轮 tokens），阶段进度见消息区 `▶` 行，思考内容在端点回传 reasoning 时以灰色斜体滚动显示末 6 行（按显示宽度折行取尾、不足补空行，块高恒定不跳动）、收束后默认折叠为摘要行 `✻ Thought for Ns`（附 [Tab 翻阅] 提示，一次入档不重绘，全文经历史翻阅展开）。

**正文 Markdown 排版**：助手答复按 Markdown 结构化渲染（markdown-it 解析为排版 IR）——标题 `#`–`######` 分级（与正文同色：1–4 级加粗、5/6 级加粗暗灰）、无序/有序列表缩进对齐、围栏代码块带语言标签并对已知语言做语法高亮（`diff`/`patch` 按增删行着色：`+` 绿 / `-` 红 / `@@` 青）、表格按终端宽度预算列宽（超宽单元格自动折行、总宽不超屏；表格随流式整块入档，不出现中途散行）、行内代码原色、加粗/斜体/删除线、引用缩进、分割线横线；模型输出的 Markdown 符号不会原样显示。

## 3. 会话命令

以下命令均不消耗模型调用（`/init` 与 `/plan` 除外，二者发起真实模型调用）：

| 命令 | 作用 | 实测行为 |
| --- | --- | --- |
| `/help` | 列出全部命令 | 追加系统消息：命令清单 |
| `/init` | 模型分析生成/完善 SUNSHINE.md | 发起一次真实模型任务：分析代码库后 write 生成（或完善既有）SUNSHINE.md，写盘经安全链；文件随每轮上下文装配自动装载 |
| `/status` | 会话与账本摘要 | 显示 `账本：N runs / M tokens；消息 X 条；待办 Y 项` |
| `/new` | 软重置新会话 | 清空消息与待办、清除「本会话放行」审批登记；记忆与账本保留 |
| `/compact` | 立即压缩上下文 | 复用自动压缩同款窗口链，显示 `已压缩：N 个摘要块重注入` |
| `/plan <目标>` | 先规划后执行 | 见第 6 节；运行中调用会提示「暂不能开始规划」，缺目标显示用法 |
| 其他 `/xxx` | 未知命令 | 提示 `未知命令：/xxx（/help 查看清单）` |

## 4. 任务执行

- **自然语言直接下达**：输入文字回车即提交。运行中继续输入会进入 FIFO 队列（落屏 `! 已排队：…` 提示），当前任务结束后依序执行。
- **多行输入**：行尾单个 `\` 回车为续行（换入下一行），整段编辑完再提交。
- **输入历史**：↑/↓ 回溯最近 100 条已提交输入（空闲/出错态可用）。
- **阶段进度行**：模型回复 JSON 可选携带 `"phase"` 字段（一句话说明当前正在做什么，1-2 行内），到达即以 `▶` 灰色进度行先于对应动作/答复上屏——用户无需等收束即可得知当前进度；未携带 `phase` 的步骤不额外落进度行（避免与 `⏺` 工具行重复）。
- **答复流式上屏**：模型输出经增量协议提取，`reply` 字段逐字上屏（协议 JSON 骨架不上屏）；正文按「安全点」切块增量入档（空行段落边界优先、围栏代码块与 GFM 表格开→闭之间不切、无边界超长段按最近换行兜底）——随生成滚动入滚动缓冲，不再等任务结束整段落屏；`done` 载荷仍为终稿权威，已入档前缀按前缀去重、尾段补齐，分块拼接无损。
- **工具事件实时可见**：模型每次调用工具，消息流追加 `⏺ VERB target` 调用行，执行后追加 `⎿ ✓/✗ summary` 结果行，无需等任务结束。
- **思考内容可见**：端点回传 `reasoning_content`/`reasoning` 时，思考流以灰色斜体滚动显示最近 6 行（过程实时反馈、块高恒定不跳动；收束即折叠不刷屏），收束后保留 `✻ Thought for Ns` 摘要行，全文经 Tab 历史翻阅展开；端点不回传则该区域整体不出现（静默降级）。
- **历史翻阅**：Tab（非斜杠输入、空闲/出错态）进入翻阅视图——块粒度回看：默认自动展开最新 3 个思考/工具块，↑/↓ 在块间移动唯一焦点（焦点块自动展开、原焦点收起，同时至多 4 块展开），视口以焦点块所在轮为末向前至多 6 轮随动；↓ 越过末块、Tab 或 Esc 即返回实时视图，继续输入退出翻阅并照常处理。
- **失败呈现**：模型/网络异常以 `! 错误：…` 落屏，状态栏转为「出错」并保留出错现场供回看；输入新任务即继续，不影响后续使用。
- **未完成终止**：触发安全网（时间上限/token 预算/步数上限）时以 `! 未完成终止：…` 说明原因（预算耗尽可续跑），已产出内容不丢。
- **Static 逐消息入档渲染**：横幅与全部消息（含当前轮，到达即入档）经 ink `Static` 一次性打印，打印后不再重绘（Claude Code 同款机制）——滚动缓冲中每条内容只出现一次，不再整帧重影；动态帧仅剩实时流预览、输入框与状态栏（零消息渲染，帧高有界且恒定），流式中间态也以终稿形态滚入滚动缓冲，跟随终端滚动即可回看全部。答复流式预览固定末 8 行且只呈现未入档尾段（已入档前缀由水位排除，不与滚动缓冲重复）；正文各块随生成即时入档。
- **耗时预期**：真实端点（GLM）下，单任务通常 20–60 秒，含多步工具调用与审批的任务更久；状态栏持续显示「运行中」即为正常。

## 5. 审批与三档权限模式

启动参数 `--mode` 决定 guard 的裁决策略：

| 模式 | 语义 | 适用场景 |
| --- | --- | --- |
| `manual`（缺省） | 只读白名单自动放行；其余操作挂起审批卡，等待键盘裁决 | 日常交互（推荐） |
| `plan` | 仅只读操作放行，写操作直接拒绝、不询问 | 只读分析/评审 |
| `dontAsk` | 不询问，自动批准未被 deny 规则拦截的操作 | 稳定测试/受信场景 |

**manual 模式审批键**（审批卡出现时按键即裁决）：

| 键 | 决策 | 效果 |
| --- | --- | --- |
| `y` | 放行一次 | 仅本次操作放行 |
| `a` | 本会话放行 | 同类操作本会话内不再询问（`/new` 可清除登记） |
| `n` | 拒绝 | 本次操作拒绝，模型会收到拒绝结果并继续/调整 |

**硬底线（任何模式生效，审批不可豁免）**：

- 只读白名单命令自动放行：`ls cat pwd grep find head tail wc which diff stat du cd echo`
- 破坏性命令直接拦截：`dd fdisk shutdown reboot poweroff halt`
- 下载执行管道（`curl/wget … | sh`）直接拦截

## 6. plan 模式：先规划后执行

```text
> /plan 创建两个文件 a.txt 和 b.txt
! 计划确认卡（/plan）            ← 规划是一次真实模型调用（Loop 长任务模板），产出系统消息计划卡
1. 创建 a.txt
2. 创建 b.txt
共 2 项，确认后逐项执行
❯ 计划待确认：y 执行 / n 放弃 ▊  ← 输入框占位分流，状态栏切换为「待确认计划」
```

- `y`：确认执行 → 计划逐条转为待办，逐项调用模型执行，完成一条勾选一条（状态栏与待办区 `待办 1/2 → 2/2`，步骤进度以 `▶ Step 1/2 — …` 行落屏）。
- `n`：放弃计划 → 回到空闲，不产生待办、不执行任何步骤。
- 规划失败兜底：模型未产出「1. xxx」编号步骤时提示原因并取消，不进入确认卡。
- 单项失败宁停不误：失败项保持未勾选，剩余步骤暂停并落屏原因，等待你的下一步指令。

## 7. 快捷键

| 按键 | 作用 |
| --- | --- |
| `Enter` | 提交输入；行尾单个 `\` 回车为续行（换入下一行），整段编辑完再提交 |
| `Tab` | `/` 前缀输入时补全斜杠命令（已是完整命令则循环下一个）；否则在空闲/出错态进入历史翻阅；翻阅态内再按即退出回实时 |
| `↑` / `↓` | 回溯输入历史（最近 100 条，空闲/出错态可用）；翻阅态下在思考/工具块间移动焦点（焦点块自动展开、视口随动滑动，↓ 越过末块回实时） |
| `←` / `→` | 输入光标左右移动（多行缓冲按字符连续跨行） |
| `Home` / `End` | 光标直接定位到缓冲首/尾（`Ctrl+A` / `Ctrl+E` 同义） |
| `Backspace` / `Delete` | 退格删除光标前字符；`Delete`（⌦）删除光标处字符 |
| `Ctrl+C` | 退出 TUI（「本会话放行」登记为进程内存态，退出即失效） |

> 等待审批/待确认计划时输入被拦截：按 `y`/`a`/`n` 或 `y`/`n` 直接裁决。

## 8. 数据与目录约定

- **root 语义**：启动时 positional 目录或缺省 `process.cwd()`，本会话所有文件操作被约束在该目录内（越界路径拒绝）。
- `.data/`：账本（runs/tokens 统计）、三级记忆、学习沉淀技能的落盘位置；`/status` 读账本，`/new` 不清账本。
- `SUNSHINE.md`：项目业务配置，会话启动时装载进模型上下文。
- 以上均不入库（.gitignore 已覆盖）。

## 9. 故障排查

| 现象 | 原因与处理 |
| --- | --- |
| 启动即报模型错误 / 答复均为错误 | `.env` 未配置或端点不可达：检查 `OPENAI_API_KEY`/`OPENAI_BASE_URL`/`OPENAI_MODEL` |
| 输入无反应 | 大概率处于「等待审批/待确认计划」：按 y/a/n 或 y/n 裁决 |
| 任务长时间「运行中」 | 多步工具调用耗时属正常；含审批的任务在等你按键 |
| 写操作总被拒 | 当前为 `plan` 模式（只读）：重启并用 `--mode=manual` 或 `dontAsk` |
| 看不到思考内容（✻） | 端点未回传 reasoning 字段（部分模型不含）；属静默降级，不影响答复与工具执行 |
| Markdown 符号偶尔原样显示 | 模型输出含解析器未覆盖的非标准语法时按字面回退（不吞字）；属降级为逐行文本，内容不丢 |
| 想回看很久之前的输出 | 终端滚动缓冲保留全部历史（Static 入档，不截断不重影）；或按 `Tab` 进翻阅视图，`↑`/`↓` 逐块移动焦点展开细看 |

## 10. 与其他 CLI 命令的关系

TUI 是交互式入口；批量/无人值守场景用同仓库 CLI：

```bash
pnpm cli selfcheck                        # 骨架自检（含 tui 事件流冒烟行）
pnpm cli run <dir> --template=test-loop --goal="目标（验收标准：id=描述）"
pnpm cli pipeline <dir> --goal="目标（验收标准：id=描述）" [--yes]
```

> `pipeline` 的 `--goal` 缺失或为空时在参数层即报错退出（「check 依赖结构化验收清单」）；使用时仍务必携带「验收标准：id=描述」结构化验收段，check 节点依赖其逐条核验。
